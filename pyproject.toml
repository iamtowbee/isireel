[tool.poetry]
name = "isireel-python"
version = "1.0.0"
description = "Python workspace for AI and server projects"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.8"

# Server dependencies
textual = "^0.47.0"
rich = "^13.7.0"
asyncio-dgram = "^2.1.2"

# AI dependencies
torch = "^2.0.0"
anthropic = "^0.25.0"
openai = "^1.0.0"
numpy = "^1.24.0"

[tool.poetry.group.dev.dependencies]
pytest = "^7.0.0"
black = "^23.0.0"
ruff = "^0.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
